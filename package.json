{
  "name": "gatsby-source-vimeo-all-download",
  "version": "0.0.1",
  "description": "gatsby source plugin to get vimeo videos and info",
  "author": "Sonarism, Inc. - Usama Minegishi <support@sonarism.com>",
  "license": "0BSD",
  "main": "gatsby-node.js",
  "scripts": {
    "build": "babel src --out-dir . --ignore **/__tests__",
    "prepare": "cross-env NODE_ENV=production npm run build",
    "watch": "babel -w src --out-dir . --ignore **/__tests__"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "vimeo"
  ],
  "peerDependencies": {
    "gatsby": "^2.0.0"
  },
  "dependencies": {
    "vimeo": "^2.1.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "babel-preset-gatsby-package": "^0.1.4",
    "cross-env": "^5.2.0"
  }
}
